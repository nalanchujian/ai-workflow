# 变更评审输出模板

写入 `<tasks-root>/<task-id>/change-review.md`。

```yaml
---
task_id: <task-id>
node: change-review
status: completed | blocked
attempt: 1
artifact_schema_version: 1
---
```

# 1. 结果

> **状态：** <完成 / 阻塞>
> **结论：** <通过 / 修复后通过 / 拒绝完成>
> **规模：** <P0/P1/P2 与范围偏差数量>

# 2. 产出

**审查范围**

- 批准范围：引用 `RF-*` / `WP-*`
- 实际范围：引用最终 diff 与 `change_fingerprint`
- 验证依据：引用 `AC-*` / `BUG-*` 或轻量验收项

**发现项**

| 发现 ID | 级别 | 文件或区域 | 问题 | 证据 | 处置 |
| --- | --- | --- | --- | --- | --- |
| RV-001 | P0 / P1 / P2 |  |  |  | 必须修复 / 接受风险 / 无需处理 |

没有发现项时写“无”。

**一致性与交付证据**

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 需求范围 / 实施计划 / 验证覆盖 / 回滚条件 |  |  |

- `repository_root`：
- `base_commit`：
- `candidate_tree`：
- `change_fingerprint`：

只记录最终差异、范围偏差和新增评审发现；上游结论一律通过 ID 或代码指纹引用。

# 3. 待确认

| ID | 类型 | 事项与影响 | Owner | 责任节点 | 完成条件 |
| --- | --- | --- | --- | --- | --- |
| RV-001 | 风险 / 缺陷 / 资料依赖 / 环境阻塞 |  |  | change-review |  |

没有未决项时写“无”。

# 4. 下一步

- 当前动作：关闭未决项；通过时由编排器展示最终完成 Gate
- 完成条件：结论为“通过”、无阻断发现、代码指纹一致
