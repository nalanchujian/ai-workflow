# 影响分析输出模板

写入 `<tasks-root>/<task-id>/impact-analysis.md`。

```yaml
---
task_id: <task-id>
node: impact-analysis
status: completed | blocked
attempt: 1
template_version: 2
---
```

# 结果

| 项目 | 内容 |
| --- | --- |
| 影响等级 | 低 / 中 / 高 |
| 影响规模 | <模块、文件候选、公共依赖和回归区域数量> |
| 核心结论 | <一句话说明真实改动边界> |

# 交付

## 影响概览

| 区域 | 处置类型 | 影响原因 | 对应需求 |
| --- | --- | --- | --- |
|  | 必须修改 / 需要回归 / 不可直接修改 / 外部依赖 |  | RF-000 |

## 改动候选

| 文件或目录 | 当前职责 | 预期影响 | 处置类型 | 证据 |
| --- | --- | --- | --- | --- |
|  |  |  | 修改 / 新增 / 删除 / 仅回归 |  |

这里只定义真实影响边界，不设计具体实现。

## 调用链

1. `<入口>` → `<状态或控制器>` → `<API/公共能力>` → `<结果消费方>`

只记录与本次需求有关的触发、映射和消费关系。

## 回归范围

| 区域 | 必测行为 | 纳入原因 | 对应用例 |
| --- | --- | --- | --- |
|  |  |  | AC-000 |

# 未决项

| ID | 类型 | 问题 | 影响 | Owner | 关闭条件 | 重试节点 |
| --- | --- | --- | --- | --- | --- | --- |
| BLK-IA-001 | 风险 / 阻塞 |  |  |  |  | impact-analysis |

没有未决项时写“无”。

# 交接

| 项目 | 内容 |
| --- | --- |
| 交付给 | `implementation-design` / 材料 Owner |
| 本节点提供 | 改动候选、调用链、公共依赖和回归范围 |
| 准入条件 | 关键入口、跨模块边界、不可直接修改项和外部依赖已有证据 |
| Gate 含义 | 确认影响边界完整，可据此设计方案；不授权修改业务代码。blocked 时不创建 Gate |
