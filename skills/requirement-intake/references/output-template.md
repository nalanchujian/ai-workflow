# 需求定义输出模板

写入 `<tasks-root>/<task-id>/requirement-analysis.md`。草稿生成后先说明本节点产出了什么，再进入逐条确认。

```yaml
---
task_id: <task-id>
node: requirement-intake
status: awaiting_confirmation | completed | blocked
attempt: 1
artifact_schema_version: 1
---
```

# 1. 结果

> **状态：** <草稿待确认 / 已完成 / 资料阻塞>
> **结论：** <一句话需求结论>
> **规模：** <事实数、确认项数>

# 2. 产出

**范围**

| 类型 | 内容 | 依据 |
| --- | --- | --- |
| 包含 |  |  |
| 排除 |  |  |
| 约束 |  |  |

**需求事实**

| 事实 ID | 模块 | 目标行为 | 当前差异 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- |
| RF-001 |  |  |  |  | 已确认 / 需确认 / 超出范围 |

每行只记录一个可观察行为；事实不写推荐答案或实现方案。

# 3. 待确认

| 问题 ID | 决策 ID | 来源事实 | 需要确认 | 采用规则 | 状态 |
| --- | --- | --- | --- | --- | --- |
| RQ-001 | CL-001 | RF-001 |  | <推荐规则或 Owner 修改后的最终规则> | 待人工确认 / 已人工确认 |

问题和决策合并在同一行，不再维护重复的 RQ 表、CL 表和未决项表。没有确认项时写“无”。

# 4. 下一步

- 当前动作：逐条确认剩余 `RQ-*`；全部确认后由编排器登记产物并展示 Gate
- 完成条件：所有 `RQ-*` 的 `CL-*` 状态均为“已人工确认”
