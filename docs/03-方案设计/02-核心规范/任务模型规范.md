# 任务模型规范

任务格式为 `aiw.task/v6`。任务创建时固定保存 `requirement-analysis`、`api-analysis`、`design-slicing`、`solution` 和 `plan` 节点，以及每个阶段锁定的技能版本。计划成功后会生成开发单元。

需求输入为未询问或已提供的 Lark 文档 URL，并可记录章节名称。接口输入为未询问、未提供或多个 YApi URL；设计输入为未询问、未提供或一张复制进任务目录的图片。

需求分析节点始终要求人工审核。接口和设计节点在用户未提供资料时以完成状态直接推进下游，因此节点图不会因资料是否存在而变化。
