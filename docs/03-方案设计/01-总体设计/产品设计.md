# 产品设计

AIW 通过 CLI 受控推进研发任务。用户只需创建任务、处理需求中的待决策事项、回答一次资料对话，并执行每一步提示的节点命令。

资料对话固定为接口文档后设计图。接口资料可输入多个 URL，设计资料只能输入一张本地 PNG/JPEG。每次回答立即保存，重启后继续未回答项目。

计划不是审批关卡。计划产物引用有效资料后自动生成开发单元；状态命令只提示 `review`、`inputs`、`run`、`ignore` 和例外的 `cancel`。
