# 架构设计

任务初始化时锁定需求分析、接口分析、设计图切割、技术方案和开发计划节点。`task run` 在执行前三个资料节点时解析命令参数并通过 `TaskInputService` 保存资料选择；接口或设计的 `--skip` 会将节点直接标记完成并解锁下游。

接口节点运行时固化每个 YApi URL 的快照；设计节点使用任务内原图切割资产。计划运行成功时由 `materializeDevelopmentWork` 原子生成开发单元上下文和节点。
