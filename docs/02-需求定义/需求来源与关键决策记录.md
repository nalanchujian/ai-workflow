# 需求来源与关键决策

MVP 以一个 HTTP(S) 需求文档 URL 创建任务。`requirement-analysis` 读取并固化该文档快照，事实登记只包含快照能直接确认的内容。

会影响本期实现、但无法由需求确定的问题进入决策登记。`task review` 逐项选择本期方案或延期；延期事项不进入技术方案和开发计划。

需求完成后，CLI 使用 `task inputs` 收集可选接口文档 URL 与一张设计图。接口和设计资料不是需求分析的输入，不能改变需求事实或决策范围。
